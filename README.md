# PostgreSQL-BigQuery-ETL-Pipeline
Implementation of a robust and scalable Data Engineering (ETL) pipeline. This project demonstrates the secure migration of transactional data from PostgreSQL (OLTP) to a BigQuery (OLAP) Data Warehouse, focusing on secure credential management (Google Secret Manager) and schema optimization for advanced analytical querying.
